{
  "name": "auth-notes-app",
  "version": "1.0.0",
  "description": "Full-stack authentication and notes application",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "cd auth-notes-client && npm run dev",
    "dev:server": "cd auth-notes-server && npm run dev",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd auth-notes-client && npm install && npm run build",
    "build:server": "cd auth-notes-server && npm install",
    "start": "node auth-notes-server/src/server.js",
    "install:all": "npm install && cd auth-notes-client && npm install && cd ../auth-notes-server && npm install",
    "render-build": "cd auth-notes-client && npm install && npm run build && cd ../auth-notes-server && npm install",
    "render-start": "node auth-notes-server/src/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}